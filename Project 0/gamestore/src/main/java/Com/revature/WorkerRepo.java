package Com.revature;

import java.util.Scanner;

import Com.revature.model.Workers;

public interface WorkerRepo { 
Scanner SC = new Scanner(System.in);
	static void addWorker(Workers workers)
	{
		
		
	}
	
      <Firstname> void save(Firstname FirstName);
        Workers addID(int workID);
        Workers addName(String firstName);
        Workers addCity(String City);
        Workers addTask(String Task);
        
}
    

 
 